# Roadmap

Next steps:

- Editable properties for nodes, including rename.
- Save and load diagram as JSON.
- Export to PNG / SVG.
- Schema validation rules for allowed connections and required elements.
- Domain expansion: hot water, solar loop and water treatment.
